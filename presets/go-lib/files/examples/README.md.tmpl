# {{ .inputs.repo_name }} examples

Runnable examples for the `{{ .inputs.package_name }}` package live here — one
`main` package per subdirectory. None exist yet; add the first alongside the
library's initial release.
