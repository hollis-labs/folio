package {{ .inputs.package_name }}

// Hello returns a greeting from the {{ .inputs.package_name }} package.
//
// This stub exists so a freshly-scaffolded module compiles and tests pass
// end-to-end; replace it with the package's real exported surface as the
// library grows.
func Hello() string {
	return "hello from {{ .inputs.package_name }}"
}
